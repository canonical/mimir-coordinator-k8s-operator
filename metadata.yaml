name: mimir-coordinator-k8s
assumes:
  - k8s-api
 
summary: Mimir coordinator

description: |
    Mimir coordinator.

requires:
  mimir-worker:
    interface: mimir_worker

  s3:
    interface: s3
    limit: 1

